{
  "_doc": [
    "发布链（check-release-gates.mjs --run --require）允许的 SKIP 声明表：**未声明或超额的 SKIP 一律判红**。",
    "为什么需要它：有些 SKIP 不是「取不到判据」，而是**该输入在发布链的时序里必然不在场**——这类必须具名写清理由，",
    "才不至于把纪律变成「谁都能顺手 SKIP」。声明是逐门禁的：max = 允许的 SKIP 计数上限。"
  ],
  "gates": {
    "check-tool-output-schema.mjs": {
      "max": 3,
      "why": "三处不适用：dsh-shell-termux / dsh-client-ui-responsive 源码无 defineTool（非工具插件），dsh-host-web-compat 无 src/index.ts——这三者没有可校的 output.schema。剩下的工具插件逐个真跑（覆盖 7 个插件 / 156 个校验分支）。"
    },
    "check-boot-budget.mjs": {
      "max": 1,
      "why": "C5 正向对照需要**打过 A5/P1 探针补丁的引擎树**，而补丁在**注入阶段**打（设备实测同源：stage 树无探针面、注入后快照有）。发布链前置门禁跑在注入之前，故该对照取不到判据（如实记 SKIP，不算绿）；C1-C4/C6 六项全部按真实产物真检。"
    }
  }
}
